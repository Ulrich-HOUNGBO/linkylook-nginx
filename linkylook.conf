server {
    listen 443 ssl http2;
    server_name linkylook.com www.linkylook.com;


    # 1) Next.js static chunks
    location ^~ /_next/static/ {
        proxy_set_header Host app.linkylook.com;    # FIXED: use a hostname
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://linkylook-web:3004;
        expires 1y;
        add_header Cache-Control "public, immutable, max-age=31536000";
    }

    # 2) Next.js image optimizer
    location ^~ /_next/image/ {
        proxy_set_header Host app.linkylook.com;    # FIXED
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://linkylook-web:3004;
    }

    # 3) Home → app root
    location = / {
        proxy_set_header Host app.linkylook.com;    # FIXED
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://linkylook-web:3004/;
    }

    # 4) Only single-segment slugs → /links/<slug>
    location ~ ^/([^/]+)$ {
        proxy_set_header Host app.linkylook.com;    # FIXED
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://linkylook-web:3004/links/$1;
        proxy_redirect off;
    }

    # 5) Fallback: forward as-is
    location / {
        proxy_set_header Host app.linkylook.com;    # FIXED
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://linkylook-web:3004;
        proxy_redirect off;
    }
}